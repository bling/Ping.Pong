<UserControl x:Class="PingPong.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="#FF111111">
        <Button x:Name="Install"
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="{StaticResource TweetTextBorderBrush}"
                BorderThickness="1"
                Content="{Binding Text}"
                FontFamily="{StaticResource DefaultFontFamily}"
                FontSize="21.333"
                Foreground="{StaticResource ButtonForegroundBrush}"
                Padding="30,20"
                Style="{StaticResource SimpleToggleButtonStyle}" />
    </Grid>
</UserControl>
